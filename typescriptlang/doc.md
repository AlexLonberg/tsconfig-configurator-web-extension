
# ⚙️ В этом каталоге:

Файл `index.html`(актуально на 01.11.2025) - копия страницы [https://www.typescriptlang.org/tsconfig/](https://www.typescriptlang.org/tsconfig/) с удаленными ссылками на подгружаемые ресурсы.

🔥 Цель этого файла - использовать _"hot-reload"_ с `Vite` для разработки `content_scripts`.

В конфигурации `vite.config.ts` следует изменить:

* `root` - Корень проекта.
* `publicDir` - Путь относительно `root` - для иконки и возможных статических ресурсов. 
* `resolve.alias` - Импортировать из `index.html` относительный модуль `<script src="../src/app.ts">` за пределами корня, не получается - изменяем на абсолютный путь `<script src="/src/app.ts">` и резолвим `/src/` в путь к каталогу источников.

```ts
{
  root: './typescriptlang',
  publicDir: './',
  resolve: {
    alias: {
      '/src/': 'C:/absolute/path/src/'
    }
  }
}
```
